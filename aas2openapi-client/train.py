# ML Algos aufrufen und auf dataframe anwenden

# 1. Definition ML ALgo
# model = xgboost.regression()

# 2. Daten laden
# data = dataframe.load()

# 3. Daten splitten
# train, test = sklear.train_test_split(data)

# 4. X, y = train["Spaltennamen"], train["Ziespalten"]

# 5. Model trainieren
# model.train/fit(X, y)

# prediction = model.predict(test_X)

# model.save(path_in_verawltungssvhale)

